p Hi #{@user.first_name},

p
  strong #{@requester.name}
  - if @requester.admin_of?(@post.company)
    = " (Staff) "
  - else
    = " (#{@requester.email}) "
  | created a new post: 
  a href=@post_url
    strong= @post.title
  |  in #{@post.board.name}

.comment
  = simple_format @post.content.parsed

br
hr
i.soft
  | Please note that this is a notification email.&nbsp;
  a href= @post_url View post on Cadet
  | &nbsp;to send a reply.
