# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

development:
  secret_key_base: 3bdc6edac354a73106622fb335448d7bb22aff858b221ad5388d6b820c4905fe8850cbad49463c1ea75f97acca48ca6a336c68053d0b492cf2f462db78083a9d
  postmark:
    reply_to_hash: ""
    inbound_domain: ""
    api_token: ""

test:
  secret_key_base: d76c227cc246fa6fa2c0269efd808f98a9c3f75f34e6f13b99115cf9ed25023e8595a28b789ce61a2f079b75cafe2991222253e474542bb049b1b5d5d58d7014
  postmark:
    reply_to_hash: ""
    inbound_domain: ""
    api_token: ""

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
