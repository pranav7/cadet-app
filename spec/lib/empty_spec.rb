require "spec_helper"

RSpec.describe "An example group the contains one empty example" do
  context "to allow us to benchmark the overhead of running a single example" do
    it "does nothing" do
    end
  end
end
